# AbuseIPDB ETL Connector

## Overview
This connector extracts IP abuse reports from the AbuseIPDB API, transforms the JSON, and loads it into a MongoDB collection.

## Setup
1. Install dependencies:
